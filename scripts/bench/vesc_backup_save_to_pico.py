"""DEPRECATED: use scripts/vesc_provision.py.

This script overlaps with the canonical provisioning workflow, which already:
  - applies limits,
  - verifies live persisted config,
  - saves MCCONF/APPCONF snapshots + metadata.
"""

print("DEPRECATED: scripts/bench/vesc_backup_save_to_pico.py")
print("Use: mpremote mount . run scripts/vesc_provision.py")
raise SystemExit(0)
"""DEPRECATED: use scripts/vesc_provision.py.

This script overlaps with the canonical provisioning workflow, which already:
  - applies limits,
  - verifies live persisted config,
  - saves MCCONF/APPCONF snapshots + metadata.
"""

print("DEPRECATED: scripts/bench/vesc_backup_save_to_pico.py")
print("Use: mpremote mount . run scripts/vesc_provision.py")
raise SystemExit(0)
